'use strict';

module.exports = (function(pEventHandler) {
	var robotPlayCards = {
	};

	// draw a card
	robotPlayCards.playCards = function(robot) {
		console.log('play cards');
		
	}
	
	return robotPlayCards;
})();